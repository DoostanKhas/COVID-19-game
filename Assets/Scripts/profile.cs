//using UnityEngine;
//using UnityEngine.UI;
//using System.IO;

//public class profile : MonoBehaviour
//{
    //public InputField inputfield;
    //public GameObject Sign_In_Panel;
    //public Text text;

    /*public void Save_Button()
    {
        File.WriteAllText(Application.persistentDataPath + "m.lvl", inputfield.text);
        Sign_In_Panel.SetActive(false);
        if (File.Exists(Application.persistentDataPath + "m.lvl"))
        {
            text.text = File.ReadAllText(Application.persistentDataPath + "m.lvl");
        }
    }
    /*public void profile_Btn()
    {
        Sign_In_Panel.SetActive(true);
    }
    void Start()
    {
        if (File.Exists(Application.persistentDataPath + "m.lvl"))
        {
            text.text = File.ReadAllText(Application.persistentDataPath + "m.lvl");
            Sign_In_Panel.SetActive(false);
        }
    }*/
//}
